// if possible, do react (tsx):
// additional features:
// toogle numbers (default)
// toogle solo leveling skins (ranked)
// difficulties (easy, med, hard, normal)
// restart
// high score
// mods: bomb (3x3), combo, chain, undo (w/ limit), delete, swap?, frozen, quantum (tile w/ different digits)